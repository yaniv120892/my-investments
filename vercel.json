{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "npm run db:deploy:if-production && npm run build",
  "crons": [
    {
      "path": "/api/snapshot",
      "schedule": "0 22 * * 1-5"
    }
  ]
}
