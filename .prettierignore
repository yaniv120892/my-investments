.next
node_modules
package-lock.json
src/prisma/migrations
public
*.log

# Captured provider responses — kept byte-faithful to what the API returned,
# so a fixture can be diffed against a fresh capture.
src/lib/providers/__tests__/fixtures

# Rules vendored from claude-config — kept byte-faithful to upstream for the
# same reason, so re-syncing one is a diff of the rule text and nothing else.
.claude/rules
